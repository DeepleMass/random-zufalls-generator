# stosegen

## compile and install

First change to project directory

```bash
cd ~/path/to/stosegen
```

For compilation run

```bash
./compile.sh
```

For installation run

```bash
 ./install.sh
 ```

For desinstallation run

```bash
./uninstall.sh
```
